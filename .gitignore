# 追跡しないもの
#
# このリポジトリは public。
# 事故で秘密が入るのは「うっかり git add -A」なので、そこを先に塞いでおく。

# 設定の保存先。中に Slack Webhook URL が生のまま入る。
#
# ふだんの書き込み先は %LOCALAPPDATA%\ClaudeDeck\ なのでここには現れない。
# ただし src/shared/appdata.mjs は LOCALAPPDATA / XDG_STATE_HOME / HOME の
# どれも無いときにアプリ直下へ倒れる（フォルダごと渡して動かす形のための逃げ道）。
# そのときの落ち先がここになる
/ClaudeDeck/

# 依存は入れない方針だが、npm を叩いた拍子にできることがある
node_modules/

# ランチャ（C#）のビルド中間物
launcher/bin/
launcher/obj/

# 配布物の組み立て場所。
# ここに同梱用の node.exe（88MB）と Setup.exe が落ちる。
# 追跡すると 2.3MB のリポジトリが一気に 100MB を超える
/build/

# 動かしているうちに出る雑多なもの
*.log
*.tmp
npm-debug.log*

# OS が勝手に置くもの
Thumbs.db
desktop.ini
.DS_Store
